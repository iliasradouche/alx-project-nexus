{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --settings=movie_backend.production_settings && (python manage.py shell --settings=movie_backend.production_settings -c \"from movies.models import Movie; import sys; sys.exit(0 if Movie.objects.count() > 0 else 1)\" || python manage.py populate_movies --settings=movie_backend.production_settings --pages 3) && gunicorn movie_backend.wsgi:application --bind 0.0.0.0:$PORT --env DJANGO_SETTINGS_MODULE=movie_backend.production_settings",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}